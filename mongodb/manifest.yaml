apiVersion: apps/v1
kind: Deployment
namespace: roboshop
metadata:
  name: mongo
  labels:
    component: mongodb
    project: roboshop
    tier: db
  annotations:
    purpose: this is mongodb manifest file.
spec:
  replicas: 2
  selector:
    matchLabels:
      component: mongodb
      project: roboshop
      tier: db
  template:
    metadata:
      name: mongodb
      labels:
        component: mongodb
        project: roboshop
        tier: db
    spec:
      containers:
      - name: mongodb
        image: jay1303/mongodb
        resources:
          requests:
            cpu: "250m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
        ports:
        - containerPort: 27017
---
apiVersion: v1
kind: Service
namespace: roboshop 
metadata:
  name: mongodb
  labels:
    component: mongodb
    project: roboshop
    tier: db 
spec:
  selector:
    component: mongodb
    project: roboshop
    tier: db
  ports:
  - port: 27017
    targetPort: 27017